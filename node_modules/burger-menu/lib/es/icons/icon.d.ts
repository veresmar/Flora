/// <reference types="react" />
declare type IconProps = {
    style?: Record<string, any>;
    className?: string;
    rotate?: number;
};
declare function convertIcon(svg: any): (props: IconProps) => JSX.Element;
export default convertIcon;
